apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: server
    version: v1
  name: server-v1
  namespace: mirror-traffic
spec:
  replicas: 1
  selector:
    matchLabels:
      app: server
      version: v1
  template:
    metadata:
      labels:
        app: server
        version: v1
    spec:
      containers:
      - image: didiyudha/server:v1
        imagePullPolicy: Always
        name: server
        ports:
          - containerPort: 8080
            protocol: TCP
